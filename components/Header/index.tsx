import React from "react"
import { SocialBar } from "../SocialBar"
import { StyledHeader } from "./style"

export const Header = () => {
    return (
        <StyledHeader>
            <SocialBar />
        </StyledHeader>
    )
}