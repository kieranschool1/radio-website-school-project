import React from "react"
import { SocialIcon, StyledSocialBar } from "./style"

import * as call from "../"

export const SocialBar = () => {
    return (
        <StyledSocialBar>
            <SocialIcon icon={} />
        </StyledSocialBar>
    )
}